<div class="search-container" id="globalsearch" onclick="toggleSearch()">
    <div class="search-box" onclick="event.stopPropagation()">
        <div class="search-header">
            <input type="search" id="term" placeholder="{{ meta.uiStrings.searchPlaceholder }}">
        </div>

        <div class="search-layout no-preview" id="search-layout">
            <!-- Left panel: Results list -->
            <div class="search-results-panel" id="search-results"></div>

            <!-- Right panel: Content preview (hidden until search starts) -->
            <div class="search-preview-panel" id="search-preview">
                <div class="preview-placeholder">
                    <i data-lucide="file-text"></i>
                    <p>{{ meta.uiStrings.searchPreviewPlaceholder | default("Select a result to preview") }}</p>
                </div>
                <div class="preview-content" id="preview-content"></div>
            </div>
        </div>

        <!-- custom: footer hint keys use search-key-hint (not search-keys) so
             searchScript.njk's Mac ⌘K rewrite, which targets .search-keys on the
             button above, does not also overwrite these. Preserve on plugin update. -->
        <footer class="search-box-footer">
            <div class="navigation-hint">
                <span class="search-key-hint">{{ meta.uiStrings.searchEnterHotkey }}</span>
                <span>{{ meta.uiStrings.searchEnterHint }}</span>
            </div>

            <div class="navigation-hint align-icon">
                <i data-lucide="arrow-up" aria-hidden="true"></i>
                <i data-lucide="arrow-down" aria-hidden="true"></i>
                <span>{{ meta.uiStrings.searchNavigateHint }}</span>
            </div>

            <div class="navigation-hint">
                <span class="search-key-hint">{{ meta.uiStrings.searchCloseHotkey }}</span>
                <span>{{ meta.uiStrings.searchCloseHint }}</span>
            </div>
        </footer>
    </div>
</div>
{%include "components/searchScript.njk"%}
